#include <iostream>
using namespace std;

int main(void) {
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

	int input_number;
	cin >> input_number;

		// 정수 x에 사용할 수 있는 연산이 3가지가 있음
	//이 연산들을 사용하여 어떤 정수 N을 1로 만들음.
	//	이때 연산을 사용하는 횟수의 최소값을 구하는 문제
	top-down방식이랑 바텀엄방식 모두 해 보기

	int a, b;
	cin >> a >> b;
	cout << "qwer";
	return 0;
}